function changeImage(img) {
    img.src = 'until-then-sigma.gif';  // Image to show when hovered
  }
  
  function restoreImage(img) {
    img.src = 'until-then-until-then-computer.gif';  // Original image
  }
  